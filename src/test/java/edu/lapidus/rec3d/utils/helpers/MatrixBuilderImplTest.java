package edu.lapidus.rec3d.utils.helpers;

import edu.lapidus.rec3d.math.ColoredImagePoint;
import org.junit.Test;

import java.util.ArrayList;
import java.util.List;
import java.util.Random;

import static org.junit.Assert.*;

/**
 * Created by Егор on 17.05.2016.
 */
public class MatrixBuilderImplTest {
    @Test
    public void normalize() throws Exception {

    }

    @Test
    public void scaleToRoot() throws Exception {
        /*MatrixBuilderImpl matrixBuilder = new MatrixBuilderImpl();
        List<ColoredImagePoint> list = new ArrayList<>();
        Random r = new Random();
        for (int i = 0; i < 30; i ++) {
            list.add(new ColoredImagePoint(-400 + r.nextInt(800), -300 + r.nextInt(600)));
        }
        matrixBuilder.scaleToRoot(list);
        assertNotNull(list);*/
    }

}